{
  "name": "creator-tools-api",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for Creator Tools MVP",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute creator_tools_db --file=./schema.sql",
    "db:migrate": "wrangler d1 migrations apply creator_tools_db"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "wrangler": "^3.24.0",
    "typescript": "^5.3.3"
  }
}